name: GSW-C Windows

on:
  pull_request:
  push:
    branches: [master]

jobs:
  run:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Compile
      run: |
        nmake

    - name: Tests
      run: |
        ./gsw_check.exe
